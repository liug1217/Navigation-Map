# 在 TDX 會員中心(https://tdx.transportdata.tw)-> 金鑰管理 建立應用程式後取得。
# 本機測試時複製這個檔案為 .env,填入實際值(.env 已加入 .gitignore,不會被 commit)。
# 部署到 Vercel 時,則是在專案的 Settings -> Environment Variables 設定同名變數。
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=
